const container = document.querySelector('.container')
const center = document.querySelector('.letter')
const dots = document.querySelectorAll('.dot')

const letterA = document.querySelector('.A')
const letterB = document.querySelector('.B')
const letterC = document.querySelector('.C')
const letterD = document.querySelector('.D')
const letterE = document.querySelector('.E')

const letterF = document.querySelector('.F')
const letterG = document.querySelector('.G')
const letterH = document.querySelector('.H')
const letterI = document.querySelector('.I')
const letterJ = document.querySelector('.J')

const letterK = document.querySelector('.K')
const letterL = document.querySelector('.L')
const letterM = document.querySelector('.M')
const letterN = document.querySelector('.N')
const letterO = document.querySelector('.O')

const letterP = document.querySelector('.P')
const letterQ = document.querySelector('.Q')
const letterR = document.querySelector('.R')
const letterS = document.querySelector('.S')
const letterT = document.querySelector('.T')

const letterU = document.querySelector('.U')
const letterV = document.querySelector('.V')
const letterW = document.querySelector('.W')
const letterX = document.querySelector('.X')
const letterY = document.querySelector('.Y')
const letterZ = document.querySelector('.Z')

const elementsA = document.querySelectorAll('.elementA')
const elementsB = document.querySelectorAll('.elementB')
const elementsC = document.querySelectorAll('.elementC')
const elementsD = document.querySelectorAll('.elementD')
const elementsE = document.querySelectorAll('.elementE')

const elementsF = document.querySelectorAll('.elementF')
const elementsG = document.querySelectorAll('.elementG')
const elementsH = document.querySelectorAll('.elementH')
const elementsI = document.querySelectorAll('.elementI')
const elementsJ = document.querySelectorAll('.elementJ')

const elementsK = document.querySelectorAll('.elementK')
const elementsL = document.querySelectorAll('.elementL')
const elementsM = document.querySelectorAll('.elementM')
const elementsN = document.querySelectorAll('.elementN')
const elementsO = document.querySelectorAll('.elementO')

const elementsP = document.querySelectorAll('.elementP')
const elementsQ = document.querySelectorAll('.elementQ')
const elementsR = document.querySelectorAll('.elementR')
const elementsS = document.querySelectorAll('.elementS')
const elementsT = document.querySelectorAll('.elementT')

const elementsU = document.querySelectorAll('.elementU')
const elementsV = document.querySelectorAll('.elementV')
const elementsW = document.querySelectorAll('.elementW')
const elementsX = document.querySelectorAll('.elementX')
const elementsY = document.querySelectorAll('.elementY')

const elementsZ = document.querySelectorAll('.elementZ')




function changeAll() {
 dots.forEach((elementA, idx) => {
    elementA.style.animationPlayState = 'running';
 })
};

function CoolEffect() {
    dots.forEach((elementA, idx) => {
       elementA.style.transform = 'scale(1.2)';
    })
   };

letterA.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsA.forEach((elementA, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementA.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterB.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsB.forEach((elementB, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementB.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterC.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsC.forEach((elementC, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementC.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterD.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsD.forEach((elementD, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementD.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterE.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsE.forEach((elementE, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementE.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterF.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsF.forEach((elementF, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementF.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterG.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsG.forEach((elementG, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementG.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

 
letterH.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsH.forEach((elementH, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementH.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

  
letterI.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsI.forEach((elementI, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementI.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

  
letterJ.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsJ.forEach((elementJ, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementJ.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

  
letterK.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsK.forEach((elementK, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementK.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


  
letterL.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsL.forEach((elementL, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementL.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterM.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsM.forEach((elementM, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementM.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterN.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsN.forEach((elementN, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementN.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterO.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsO.forEach((elementO, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementO.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});



letterP.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsP.forEach((elementP, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementP.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterQ.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsQ.forEach((elementQ, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementQ.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterR.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsR.forEach((elementR, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementR.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterS.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsS.forEach((elementS, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementS.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterT.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsT.forEach((elementT, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementT.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterU.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsU.forEach((elementU, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementU.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterV.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsV.forEach((elementV, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementV.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});

letterW.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsW.forEach((elementW, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementW.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});



letterX.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsX.forEach((elementX, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementX.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterY.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsY.forEach((elementY, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementY.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});


letterZ.addEventListener('mouseover', () => {
    // center.style.opacity = '0'
    elementsZ.forEach((elementZ, idx) => {
        // setTimeout(() => {
            //elementA.classList.add('running');
            elementZ.style.animationPlayState = 'running';
        // }, idx * 500);
    });
});
